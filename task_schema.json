{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Task",
  "description": "A task in the ToDo application",
  "type": "object",
  "properties": {
    "title": {
      "type": "string",
      "description": "The title of the task"
    },
    "details": {
      "type": "string",
      "description": "Additional details about the task"
    },
    "completed": {
      "type": "boolean",
      "description": "Indicates whether the task has been completed"
    },
    "user": {
      "type": "string",
      "pattern": "^/users/[a-zA-Z0-9]+$",
      "description": "A Firestore document reference to the user who created the task"
    },
    "created": {
      "type": "string",
      "format": "date-time",
      "description": "The timestamp when the task was created"
    }
  },
  "required": ["title", "completed", "user", "created"]
}
